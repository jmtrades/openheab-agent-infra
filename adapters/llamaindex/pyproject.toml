[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "openheab-llamaindex"
version = "0.1.0"
description = "OpenHeab adapter for LlamaIndex."
readme = "README.md"
license = { text = "Apache-2.0" }
requires-python = ">=3.9"
dependencies = ["openheab>=0.1.0", "llama-index-core>=0.10.0"]

[tool.setuptools.packages.find]
where = ["."]
include = ["openheab_llamaindex*"]
